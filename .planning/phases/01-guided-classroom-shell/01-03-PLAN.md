---
phase: 01-guided-classroom-shell
plan: "03"
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/domain/simulation.ts
  - src/domain/simulation.test.ts
  - src/features/input-panel/InputPanel.tsx
  - src/features/input-panel/InputPanel.test.tsx
  - src/features/lesson-panel/LessonPanel.tsx
  - src/features/lesson-panel/LessonPanel.test.tsx
  - src/features/lesson-panel/ResetScenarioDialog.tsx
  - src/features/lesson-panel/ResetFlow.test.tsx
  - src/features/visualization-panel/VisualizationPanel.tsx
  - src/App.tsx
  - src/App.test.tsx
  - src/index.css
autonomous: false
requirements:
  - SCEN-04
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
  - QUAL-02
  - QUAL-06
must_haves:
  truths:
    - "D-09: Start Search moves from registered setup directly to tokenization."
    - "D-10: Every position shows Step n of total, the current title, and an accessible progress bar."
    - "D-11 and D-12: Previous and Next move sequentially only; indicators are not clickable, and both boundary controls remain visible with explicit disabled states."
    - "D-08: Reset prompts only after query or document edits; an unchanged scenario resets immediately."
    - "D-07: Confirmed reset restores query, documents, vectors, selected-scenario defaults, and setup position."
    - "D-13, D-15, D-16, and D-17: Numbered stable-ID document cards use one Edited status, expose no add/remove controls, and work for arbitrary scenario-owned document counts."
    - "D-14: Query and document textareas auto-grow from 88px to 160px before their own text overflow activates and the page continues scrolling."
    - "D-01 through D-04: The complete shell preserves the exact three-panel projector hierarchy, whole-page scroll, and bright instructional visual system."
    - "All edited text is rendered as plain text, the reset modal is keyboard operable, and the final static build needs no backend."
  artifacts:
    - path: "src/features/lesson-panel/LessonPanel.tsx"
      provides: "Setup/start state, explanations, progress, and sequential navigation"
      exports: ["LessonPanel"]
    - path: "src/features/lesson-panel/ResetScenarioDialog.tsx"
      provides: "Accessible dirty-reset confirmation"
      exports: ["ResetScenarioDialog"]
    - path: "src/features/visualization-panel/VisualizationPanel.tsx"
      provides: "Setup and registered-step preview workspace"
      exports: ["VisualizationPanel"]
    - path: "src/features/lesson-panel/LessonPanel.test.tsx"
      provides: "FLOW-01 through FLOW-04 component coverage"
      contains: "Start Search"
    - path: "src/features/lesson-panel/ResetFlow.test.tsx"
      provides: "SCEN-04 and D-08 reset behavior coverage"
      contains: "Reset edited scenario?"
    - path: "src/App.test.tsx"
      provides: "Complete guided classroom flow integration coverage"
      contains: "Previous step"
  key_links:
    - from: "src/features/lesson-panel/LessonPanel.tsx"
      to: "src/domain/simulation.ts"
      via: "derived progress/boundary selectors and reducer actions"
      pattern: "started|previousStep|nextStep"
    - from: "src/App.tsx"
      to: "src/features/lesson-panel/ResetScenarioDialog.tsx"
      via: "derived dirty state gates reset confirmation"
      pattern: "selectIsEdited"
    - from: "src/App.tsx"
      to: "src/features/visualization-panel/VisualizationPanel.tsx"
      via: "active registered step metadata"
      pattern: "activeStep"
    - from: "src/features/input-panel/InputPanel.tsx"
      to: "src/domain/simulation.ts"
      via: "query/document dispatch and reset request"
      pattern: "documentChanged|queryChanged"
---

<objective>
Complete the three-panel classroom shell with registered lesson navigation, synchronized progress, safe reset behavior, exact focus/copy contracts, and end-of-phase static verification.

Purpose: Turn the editable scenario workbench into the complete Phase 1 classroom flow while preserving all locked decisions and creating stable insertion points for later teaching views.
Output: Fully usable Phase 1 shell, component/integration coverage for SCEN-04 and FLOW-01 through FLOW-04, accessible reset confirmation, and verified static release artifact.
</objective>

## Phase Goal

**As a** high school student, **I want to** open a scenario-driven search lesson, edit its source material, navigate its steps, and reset safely, **so that** I can inspect how a search engine processes a query in class.

<execution_context>
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/workflows/execute-plan.md
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-guided-classroom-shell/01-CONTEXT.md
@.planning/phases/01-guided-classroom-shell/01-RESEARCH.md
@.planning/phases/01-guided-classroom-shell/01-VALIDATION.md
@.planning/phases/01-guided-classroom-shell/01-PATTERNS.md
@.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md
@.planning/phases/01-guided-classroom-shell/01-01-SUMMARY.md
@.planning/phases/01-guided-classroom-shell/01-02-SUMMARY.md
@docs/PRD.md

<interfaces>
From src/domain/simulation.ts after Plan 01-02:
- Scenario, SearchDocument, Vector2D, ScenarioVectors, StepId, LessonStep, SimulationSession, and SimulationAction are the canonical types.
- buildSessionFromScenario(scenario) creates independent editable state.
- simulationReducer handles scenarioSelected, queryChanged, documentChanged, started, nextStep, previousStep, and resetConfirmed.
- selectIsEdited, selectCurrentStepIndex, selectCanGoPrevious, selectCanGoNext, and selectProgress are derived selectors.

From src/content/lessonSteps.ts after Plan 01-02:
- lessonSteps contains setup plus ten stable course steps.
- firstLessonStepId is tokenization.

From src/App.tsx after Plan 01-02:
- App owns the reducer and passes typed state/callbacks to InputPanel.
- Existing live-region and focus behavior for scenario selection must remain intact.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deliver the complete registered lesson navigation and visualization path</name>
  <read_first>
    <file>.planning/phases/01-guided-classroom-shell/01-CONTEXT.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md</file>
    <file>src/content/lessonSteps.ts</file>
    <file>src/domain/simulation.ts</file>
    <file>src/App.tsx</file>
    <file>src/App.test.tsx</file>
  </read_first>
  <files>src/domain/simulation.ts, src/domain/simulation.test.ts, src/features/lesson-panel/LessonPanel.tsx, src/features/lesson-panel/LessonPanel.test.tsx, src/features/visualization-panel/VisualizationPanel.tsx, src/App.tsx, src/App.test.tsx</files>
  <behavior>
    - "Start Search at setup dispatches started and displays tokenization per D-09."
    - "Next and Previous follow lessonSteps order and never move outside its bounds per D-11."
    - "Previous is visible and disabled at setup and the first reachable lesson boundary; Next is visible and disabled at final-comparison per D-12."
    - "Every active position displays Step n of 11, its exact title, and progress aria values min 1, max 11, now n per D-10."
    - "Changing steps focuses the new step heading without scrolling the page to the top."
  </behavior>
  <action>Write LessonPanel and integration tests first for the complete setup, start, next, previous, final-boundary, title, ordinal, progress, and focus flow. Implement LessonPanel from the registry rather than conditionals per step: setup uses the exact Start Search copy; started positions render the current title, the registry preview, two short labeled explanation blocks answering What did the search engine do? and Why does it matter?, an official Progress component, and a navigation row. Progress indicators are text/bar status only and must not be buttons or links per D-11. Keep Previous step and Next step visible with native disabled attributes at boundaries per D-12. Implement VisualizationPanel so setup uses the exact Your search workspace copy and later positions show {Step title} is ready, the exact registered-step preview body, and a labeled diagram frame without asserting that calculations exist. Wire both panels to reducer state in App. After navigation, focus a programmatically focusable heading with preventScroll behavior; preserve normal page position. Keep the single 160ms opacity transition only for visualization content and disable it under reduced motion. Do not implement Run All, global keyboard shortcuts, ranking calculations, clickable step indicators, or a router.</action>
  <verify>
    <automated>npm run test -- --run src/domain/simulation.test.ts src/features/lesson-panel/LessonPanel.test.tsx src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - "The setup-to-tokenization start path, every sequential step, reverse navigation, and both boundaries pass automated tests."
    - "The current title, Step n of 11 text, and progress aria values always refer to the same registry item."
    - "No progress indicator is interactive and no navigation button disappears at a boundary."
    - "The visualization panel always has meaningful setup or registered-step content; it is never blank."
    - "Step previews use the exact UI-SPEC copy and never claim Phase 2-5 calculations."
  </acceptance_criteria>
  <done>A student can start and traverse the entire registered lesson sequence in both directions with synchronized progress, focus, explanations, and visualization previews.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete safe reset, textarea growth, and exact interaction semantics</name>
  <read_first>
    <file>.planning/phases/01-guided-classroom-shell/01-CONTEXT.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md</file>
    <file>src/domain/simulation.ts</file>
    <file>src/features/input-panel/InputPanel.tsx</file>
    <file>src/features/input-panel/InputPanel.test.tsx</file>
    <file>src/App.tsx</file>
  </read_first>
  <files>src/features/input-panel/InputPanel.tsx, src/features/input-panel/InputPanel.test.tsx, src/features/lesson-panel/ResetScenarioDialog.tsx, src/features/lesson-panel/ResetFlow.test.tsx, src/App.tsx, src/App.test.tsx, src/index.css</files>
  <behavior>
    - "Reset on an unchanged scenario immediately restores setup and does not open a dialog per D-08."
    - "Reset after a query or document edit opens Reset edited scenario?, initially focuses Keep edits, and leaves state unchanged when canceled."
    - "Confirming Reset scenario restores query, documents, vectors, selected-scenario defaults, and setup, then focuses Query and announces Scenario reset to its original values."
    - "Escape closes the dialog and restores focus to Reset scenario."
    - "Query and document textareas grow from 88px through 160px, then use their own text overflow without creating panel scroll containers per D-14."
  </behavior>
  <action>Implement reset intent in App from the derived selectIsEdited value. If false, dispatch resetConfirmed immediately; if true, open an official shadcn AlertDialog using the exact title, body, Keep edits, and Reset scenario copy. Set initial dialog focus to Keep edits, trap focus through the Radix primitive, close on Escape, restore focus to the Reset scenario trigger on cancel/escape, and after confirmation focus the Query field and update a polite live region with the exact reset announcement. Confirmed reset must use the existing reducer reconstruction path so vectors and active step cannot drift from D-07. Implement a small reusable textarea resize handler inside InputPanel that resets height to auto, sets height to the smaller of scrollHeight and 160px, starts fields at 88px, and uses internal textarea overflow only above 160px; do not animate height. Preserve stable-ID numbered cards, the single Edited badge/reset row, and no add/remove controls per D-13 through D-17. Write ResetFlow and InputPanel tests before behavior changes, including unchanged reset, dirty cancel, dirty confirm, vector restoration through reducer assertion, Escape focus return, query focus after confirm, arbitrary document count, and HTML-shaped literal text.</action>
  <verify>
    <automated>npm run test -- --run src/features/input-panel/InputPanel.test.tsx src/features/lesson-panel/ResetFlow.test.tsx src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - "Unchanged reset is immediate and dirty reset is the only path that opens AlertDialog."
    - "Keep edits is initially focused; cancel and Escape preserve edits and return focus to Reset scenario."
    - "Confirm restores all selected scenario defaults and setup, focuses Query, and announces the exact reset message."
    - "All textareas honor the exact 88px minimum and 160px maximum growth contract without page jumps or animated height."
    - "Edited content is rendered only as textarea values or React text nodes; no raw HTML API exists."
  </acceptance_criteria>
  <done>A student can reset safely from any reached step, with confirmation only for real edits and with fully keyboard-operable focus behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the complete projector shell and run the phase acceptance gate</name>
  <read_first>
    <file>.planning/phases/01-guided-classroom-shell/01-VALIDATION.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md</file>
    <file>.planning/phases/01-guided-classroom-shell/SKELETON.md</file>
    <file>package.json</file>
    <file>src/App.tsx</file>
    <file>src/index.css</file>
    <file>src/App.test.tsx</file>
  </read_first>
  <files>src/App.tsx, src/App.test.tsx, src/index.css</files>
  <action>Perform a final contract pass over the assembled page. Ensure the semantic order is header, main, Search inputs, Lesson steps, Visualization; visual order and tab order match; all fields have persistent labels; status, selection, disabled boundaries, and progress use visible text in addition to color; Lucide icons adjacent to text are aria-hidden; and no icon-only action exists. Preserve the exact 28/24/48 grid, 16px gap, 24px page padding, 1600px maximum width, top-aligned unequal-height panels, one browser scroll, UI-SPEC type sizes/weights, reserved accent usage, and reduced-motion override. Add or refine App integration coverage for load, select scenario, edit query, edit one document, Start Search, Next, Previous, unchanged reset, dirty cancel, dirty confirm, and final boundary. Run npm metadata checks for react, react-dom, typescript, vite, @vitejs/plugin-react, tailwindcss, @tailwindcss/vite, vitest, @testing-library/react, @testing-library/user-event, jsdom, shadcn, and all lockfile-added shadcn runtime packages; present their resolved package names, versions, repositories, and postinstall metadata in the execution summary for the consolidated human identity review. Do not add new packages during this gate.</action>
  <verify>
    <automated>npm run test -- --run; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $matches = rg -n "dangerouslySetInnerHTML|innerHTML|fetch\\(|axios|express|next/|prisma|supabase|firebase|add document|remove document" src package.json; if ($LASTEXITCODE -eq 0) { $matches; throw "Prohibited implementation found" }; if ($LASTEXITCODE -ne 1) { exit $LASTEXITCODE }</automated>
    <human-check>At the end of the phase, review the executor-reported package identities against the official repositories, then run the production preview at a 1280px-or-wider viewport. Confirm projector-readable text and contrast, the prescribed panel hierarchy and single page scroll, visible keyboard focus, understandable disabled boundaries without color, scenario selection, editing, Start Search, Previous/Next, and both dirty-reset cancel/confirm focus paths. Approve only if the package identities are official and the visual/dialog checks match 01-UI-SPEC.md.</human-check>
  </verify>
  <acceptance_criteria>
    - "The full Vitest suite and Vite production build pass in under the validation strategy's 60-second feedback target."
    - "Static scans show no backend, auth, database, network scenario loading, raw HTML rendering, or document add/remove implementation."
    - "The full guided flow is covered by user-facing role/label assertions rather than component internals."
    - "The consolidated package identity, projector-readability, keyboard-focus, and dialog review is approved."
    - "The generated dist site performs the complete Phase 1 flow without a backend."
  </acceptance_criteria>
  <done>Phase 1 meets every automated and manual acceptance gate for a readable, accessible, tested, static classroom shell.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | Important symbols or contract |
|---|---|
| `src/features/lesson-panel/LessonPanel.tsx` | `LessonPanel`, setup/start UI, step explanations, progress, Previous/Next |
| `src/features/lesson-panel/LessonPanel.test.tsx` | FLOW-01, FLOW-02, FLOW-03, FLOW-04 and D-09 to D-12 coverage |
| `src/features/lesson-panel/ResetScenarioDialog.tsx` | `ResetScenarioDialog`, exact AlertDialog copy and focus behavior |
| `src/features/lesson-panel/ResetFlow.test.tsx` | SCEN-04, D-07, D-08, cancel/confirm/Escape/focus coverage |
| `src/features/visualization-panel/VisualizationPanel.tsx` | `VisualizationPanel`, setup workspace and registered-step previews |
| `src/features/input-panel/InputPanel.tsx` | Final auto-growing source editors and reset trigger |
| `src/features/input-panel/InputPanel.test.tsx` | D-13 through D-17 and plain-text rendering coverage |
| `src/domain/simulation.ts` | Final bounded navigation and reset contracts |
| `src/domain/simulation.test.ts` | Registry-bound navigation and complete reset unit coverage |
| `src/App.tsx` | Complete three-panel composition, live regions, focus orchestration |
| `src/App.test.tsx` | Full Phase 1 guided-flow integration coverage |
| `src/index.css` | Final UI-SPEC layout, typography, focus, overflow, and reduced-motion rules |

## Multi-Source Coverage Audit

| Source | ID | Feature or constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Select a scenario, edit source text, navigate the lesson shell, and reset safely in a static classroom app | 01-01, 01-02, 01-03 | COVERED |
| REQ | SCEN-01 | Select complete built-in teaching scenarios | 01-02 | COVERED |
| REQ | SCEN-02 | Edit active query through canonical state | 01-02 | COVERED |
| REQ | SCEN-03 | Edit every active document by stable ID | 01-02 | COVERED |
| REQ | SCEN-04 | Restore query, documents, vectors, and setup safely | 01-03 | COVERED |
| REQ | FLOW-01 | Start from setup | 01-03 | COVERED |
| REQ | FLOW-02 | Move to next registered step | 01-03 | COVERED |
| REQ | FLOW-03 | Return to previous registered step | 01-03 | COVERED |
| REQ | FLOW-04 | Show current step and overall progress | 01-03 | COVERED |
| REQ | QUAL-02 | Projector-readable contrast and text | 01-01, 01-03 | COVERED |
| REQ | QUAL-06 | Static browser application without backend | 01-01, 01-03 | COVERED |
| RESEARCH | — | Vite React TypeScript, Tailwind 4, official components, and Vitest | 01-01 | COVERED |
| RESEARCH | — | Immutable scenario snapshots and complete session projection | 01-02 | COVERED |
| RESEARCH | — | Pure discriminated reducer and derived selectors | 01-02 | COVERED |
| RESEARCH | — | Stable data-driven step registry | 01-02, 01-03 | COVERED |
| RESEARCH | — | Plain-text input rendering and no dangerouslySetInnerHTML | 01-02, 01-03 | COVERED |
| RESEARCH | — | Accessible modal semantics and focus restoration | 01-03 | COVERED |
| RESEARCH | — | Package legitimacy checks and lockfile | 01-01, 01-03 | COVERED |
| RESEARCH | — | Nyquist reducer, component, integration, and static-build validation | 01-01, 01-02, 01-03 | COVERED |
| CONTEXT | D-01 | Three panels with visualization widest | 01-01, 01-03 | COVERED |
| CONTEXT | D-02 | Inputs left, lesson center, visualization right | 01-01, 01-03 | COVERED |
| CONTEXT | D-03 | Whole-page vertical scroll | 01-01, 01-03 | COVERED |
| CONTEXT | D-04 | Bright projector-safe instructional workspace | 01-01, 01-03 | COVERED |
| CONTEXT | D-05 | Scenario switch immediately discards edits | 01-02 | COVERED |
| CONTEXT | D-06 | Scenario switch returns to setup | 01-02 | COVERED |
| CONTEXT | D-07 | Reset restores complete immutable defaults | 01-02, 01-03 | COVERED |
| CONTEXT | D-08 | Confirm reset only when content was edited | 01-03 | COVERED |
| CONTEXT | D-09 | Start Search enters first lesson step | 01-03 | COVERED |
| CONTEXT | D-10 | Step number, title, and progress bar | 01-03 | COVERED |
| CONTEXT | D-11 | Sequential Previous and Next only | 01-03 | COVERED |
| CONTEXT | D-12 | Visible disabled boundary controls | 01-03 | COVERED |
| CONTEXT | D-13 | Compact numbered stable-ID document cards | 01-02, 01-03 | COVERED |
| CONTEXT | D-14 | Textareas auto-grow to a maximum | 01-03 | COVERED |
| CONTEXT | D-15 | One panel-level Edited status and reset | 01-02, 01-03 | COVERED |
| CONTEXT | D-16 | Fixed scenario-owned collection, no add/remove UI | 01-02, 01-03 | COVERED |
| CONTEXT | D-17 | No hard-coded document count | 01-02, 01-03 | COVERED |

Deferred document add/remove controls, tablet/reflow work, completed keyword/semantic calculations, Run All, global navigation shortcuts, and final comparison behavior are excluded because their source artifacts assign them beyond Phase 1.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user input to UI | Query and document text is untrusted and must remain plain text. |
| reducer to registered views | Canonical state and stable step metadata drive all rendered progress and navigation. |
| modal to keyboard user | AlertDialog temporarily owns focus and must release it predictably. |
| npm registry to static bundle | Audited dependencies and generated shadcn runtime packages enter the production bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering / Elevation | InputPanel, App, VisualizationPanel | mitigate | Controlled values and React text nodes only; tests use HTML-shaped strings; static scan forbids raw HTML APIs. |
| T-01-02 | Tampering | npm/shadcn dependency graph | mitigate | Preserve lockfile, rerun identity/repository/postinstall checks, and require consolidated human approval. |
| T-01-03 | Denial of Service | ResetScenarioDialog | mitigate | Official AlertDialog focus trap, initial safe focus, Escape close, trigger restoration, and automated focus tests. |
| T-01-04 | Tampering | lessonSteps and reducer bounds | mitigate | Stable StepId registry, derived indexes, and tests for first/final boundaries prevent invalid navigation state. |
| T-01-SC | Tampering | npm package supply chain | mitigate | Research audit, preinstall metadata checks, official registry restriction, package-lock, and end-of-phase human identity review. |
</threat_model>

<verification>
- `npm run test -- --run src/domain/simulation.test.ts`
- `npm run test -- --run src/features/input-panel`
- `npm run test -- --run src/features/lesson-panel`
- `npm run test -- --run src/App.test.tsx`
- `npm run test -- --run && npm run build`
- Human check: package identities, 1280px-plus projector readability, one-page scroll, visible focus, disabled-state clarity, and reset dialog focus/cancel/confirm behavior.
</verification>

<success_criteria>
- SCEN-04 and FLOW-01 through FLOW-04 pass automated user-facing tests.
- All D-01 through D-17 decisions are implemented and traceable across the three plans.
- The shell is fully usable with keyboard Tab, Shift+Tab, Enter, Space, and Escape behavior defined by native controls and AlertDialog.
- State is never communicated by color alone and all exact UI-SPEC copy/layout contracts are present.
- `npm run test -- --run && npm run build` passes, and the resulting dist site needs no backend.
- The consolidated end-of-phase package, projector, and dialog verification is approved.
</success_criteria>

<output>
Create `.planning/phases/01-guided-classroom-shell/01-03-SUMMARY.md` when done.
</output>

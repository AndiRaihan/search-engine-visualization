---
phase: 01-guided-classroom-shell
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/content/scenarios.ts
  - src/content/lessonSteps.ts
  - src/domain/simulation.ts
  - src/domain/simulation.test.ts
  - src/features/input-panel/InputPanel.tsx
  - src/features/input-panel/InputPanel.test.tsx
  - src/App.tsx
  - src/App.test.tsx
autonomous: true
requirements:
  - SCEN-01
  - SCEN-02
  - SCEN-03
must_haves:
  truths:
    - "D-05 and D-06: Selecting a different built-in scenario immediately discards prior edits, loads the new defaults, and returns to setup without confirmation."
    - "A student can select one of the five canonical teaching scenarios and see its predefined query, documents, vectors, and learning goal."
    - "A student can edit the active query and any document through controlled fields backed by one reducer session."
    - "D-13, D-15, and D-16: Documents appear as numbered stable-ID cards, the panel uses one Edited status, and no add/remove controls exist."
    - "D-07 and D-17: The session factory clones complete immutable defaults and all document behavior iterates the scenario-owned array without a fixed count."
  artifacts:
    - path: "src/content/scenarios.ts"
      provides: "Five immutable built-in scenario snapshots"
      exports: ["scenarios", "getScenarioById"]
    - path: "src/content/lessonSteps.ts"
      provides: "Stable full-course step registry"
      exports: ["lessonSteps", "firstLessonStepId"]
    - path: "src/domain/simulation.ts"
      provides: "Typed scenario/session contracts, reducer, factory, and derived selectors"
      exports: ["Scenario", "SearchDocument", "Vector2D", "ScenarioVectors", "StepId", "SimulationSession", "SimulationAction", "buildSessionFromScenario", "simulationReducer", "selectIsEdited"]
    - path: "src/features/input-panel/InputPanel.tsx"
      provides: "Scenario selection and controlled source editing"
      exports: ["InputPanel"]
    - path: "src/domain/simulation.test.ts"
      provides: "Immutable scenario switch, reset, and arbitrary-length document coverage"
      contains: "simulationReducer"
    - path: "src/features/input-panel/InputPanel.test.tsx"
      provides: "User-facing scenario and edit coverage"
      contains: "Edited"
  key_links:
    - from: "src/content/scenarios.ts"
      to: "src/domain/simulation.ts"
      via: "Scenario typed immutable records"
      pattern: "satisfies readonly Scenario"
    - from: "src/App.tsx"
      to: "src/domain/simulation.ts"
      via: "useReducer with buildSessionFromScenario"
      pattern: "useReducer\\(simulationReducer"
    - from: "src/features/input-panel/InputPanel.tsx"
      to: "src/domain/simulation.ts"
      via: "typed reducer dispatch callbacks and derived Edited state"
      pattern: "scenarioSelected|queryChanged|documentChanged"
---

<objective>
Replace the walking-skeleton source state with immutable local scenarios and a typed reducer-backed editable classroom workbench.

Purpose: Deliver SCEN-01 through SCEN-03 as a real browser capability while establishing the stable data and state contracts consumed by every later search phase.
Output: Five canonical scenario snapshots, full stable step IDs, pure session factory/reducer/selectors, and a working scenario/query/document input slice.
</objective>

## Phase Goal

**As a** high school student, **I want to** open a scenario-driven search lesson, edit its source material, navigate its steps, and reset safely, **so that** I can inspect how a search engine processes a query in class.

<execution_context>
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/workflows/execute-plan.md
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-guided-classroom-shell/01-CONTEXT.md
@.planning/phases/01-guided-classroom-shell/01-RESEARCH.md
@.planning/phases/01-guided-classroom-shell/01-VALIDATION.md
@.planning/phases/01-guided-classroom-shell/01-PATTERNS.md
@.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md
@.planning/phases/01-guided-classroom-shell/01-01-SUMMARY.md
@docs/PRD.md

<interfaces>
Plan 01-01 provides a default-exported App component, the exact visual tokens in src/index.css, official shadcn Select/Textarea/Label/Card/Badge/Button primitives, and the passing setup-to-Tokenization App test. Extend those contracts; do not replace the static Vite architecture or visual system.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deliver built-in scenario selection through immutable typed session state</name>
  <read_first>
    <file>.planning/phases/01-guided-classroom-shell/01-CONTEXT.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-RESEARCH.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md</file>
    <file>docs/PRD.md</file>
    <file>src/App.tsx</file>
    <file>src/App.test.tsx</file>
  </read_first>
  <files>src/content/scenarios.ts, src/content/lessonSteps.ts, src/domain/simulation.ts, src/domain/simulation.test.ts, src/features/input-panel/InputPanel.tsx, src/features/input-panel/InputPanel.test.tsx, src/App.tsx, src/App.test.tsx</files>
  <behavior>
    - "Selecting Common Words Are Noisy after editing another scenario loads its own exact defaults and activeStepId setup without confirmation per D-05 and D-06."
    - "Every built-in scenario exposes an id, title, description, learningGoal, defaultQuery, readonly documents, and query/document vectors."
    - "Building two sessions from one scenario produces independent document arrays and vector maps; mutating one session cannot alter the source snapshot or the other session per D-07."
    - "A scenario with a different document-array length is accepted by the factory, reducer, selector, and renderer per D-17."
  </behavior>
  <action>Define the public domain contracts in src/domain/simulation.ts: SearchDocument with stable id, title, and text; Vector2D; ScenarioVectors with query and documents keyed by document ID; Scenario; the full StepId union; SimulationSession; and discriminated SimulationAction. Define the stable lessonSteps registry in canonical order as setup, tokenization, matching, term-frequency, inverse-document-frequency, tf-idf, keyword-ranking, keyword-limitation, meaning-vectors, semantic-ranking, and final-comparison. Each registry item carries id, title, phase-appropriate two-sentence-or-shorter preview copy, and kind setup or preview; firstLessonStepId is tokenization. In scenarios.ts create the five PRD scenarios with IDs exact-match-fails, common-words-noisy, tfidf-ranking, keyword-misses-meaning, and meaning-map-distance; use the PRD titles and learning goals, query the iphone for the first three and apple phone for the last two. Each scenario uses the seven canonical documents in PRD order with IDs doc-1 through doc-7 and texts exactly as listed in docs/PRD.md lines 133-139. Give every scenario complete vectors: query [0.88,0.78] for the iphone or [0.80,0.70] for apple phone, and doc-1 [0.90,0.80], doc-2 [0.65,0.72], doc-3 [0.82,0.75], doc-4 [0.88,0.77], doc-5 [0.60,0.70], doc-6 [0.84,0.74], doc-7 [0.70,0.10]. Freeze or readonly-type the exported snapshots. Implement buildSessionFromScenario as a deep-enough projection that clones documents and both vector levels. Implement scenarioSelected so it obtains a known scenario by ID, rebuilds the whole session, and returns to setup immediately per D-05 and D-06. Write unit and component tests before implementation, then replace the Plan 01 static left region with InputPanel scenario selection using the official Select and visible Selected text. After selection, focus the setup heading and update a polite live region with the exact UI-SPEC loaded announcement. Unknown IDs must preserve current state and expose the UI-SPEC error copy rather than throw.</action>
  <verify>
    <automated>npm run test -- --run src/domain/simulation.test.ts src/features/input-panel/InputPanel.test.tsx src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - "Five selectable scenario options render with title and learning goal, and all source records include complete vectors."
    - "Switching scenarios is immediate, discards edits, returns to setup, moves focus to the setup heading, and announces the loaded scenario."
    - "Source snapshots remain unchanged after session edits and repeated switches."
    - "No reducer, selector, test, or component assumes seven documents by index or tuple length."
    - "The original Start Search walking interaction remains operational after state moves into useReducer."
  </acceptance_criteria>
  <done>A student can switch among five local scenarios and always receives an independent, complete, setup-position session through the final domain contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add controlled query and document editing with one derived Edited state</name>
  <read_first>
    <file>.planning/phases/01-guided-classroom-shell/01-CONTEXT.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md</file>
    <file>src/domain/simulation.ts</file>
    <file>src/content/scenarios.ts</file>
    <file>src/features/input-panel/InputPanel.tsx</file>
    <file>src/features/input-panel/InputPanel.test.tsx</file>
  </read_first>
  <files>src/domain/simulation.ts, src/domain/simulation.test.ts, src/features/input-panel/InputPanel.tsx, src/features/input-panel/InputPanel.test.tsx, src/App.tsx, src/App.test.tsx</files>
  <behavior>
    - "Editing the query changes only session.query and makes selectIsEdited true."
    - "Editing doc-4 changes only the document with id doc-4 and preserves all IDs and array order."
    - "Restoring edited text exactly to scenario defaults makes selectIsEdited false without dispatching a dirty-state action."
    - "Dispatching resetConfirmed reconstructs query, documents, vectors, and setup position from the selected immutable scenario per D-07."
    - "Text containing HTML-like markup is displayed and edited as literal plain text; no HTML execution path exists."
  </behavior>
  <action>Extend simulationReducer with queryChanged, documentChanged, started, nextStep, previousStep, and resetConfirmed actions using the registry order and stable step IDs. Keep the reducer pure; derive selectIsEdited, current index, canGoPrevious, canGoNext, and progress from session plus immutable sources instead of storing duplicate booleans or percentages. Implement controlled Query and Documents sections in InputPanel with persistent labels, official Textarea and Card primitives, Document {n} visible numbering, React keys and updates by stable document ID, and no add/remove affordance per D-13, D-16, and D-17. Show exactly one panel-level Edited badge in the reserved status row when selectIsEdited is true per D-15. Do not render a Reset scenario control in this plan; Plan 01-03 adds the control and its complete D-08 confirmation behavior atomically so no dead or incorrect reset action is exposed. Render all query/document values as React text and textarea values; never use dangerouslySetInnerHTML, parsing, Markdown rendering, or raw DOM HTML assignment. Preserve the five-scenario selection and existing Start Search interaction. Add tests for query edits, single-document edits, arbitrary document counts, derived dirty state returning to false, reset reconstruction including vectors, bounded navigation selectors, and literal rendering of an XSS-shaped string.</action>
  <verify>
    <automated>npm run test -- --run src/domain/simulation.test.ts src/features/input-panel/InputPanel.test.tsx src/App.test.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - "The query and every scenario-owned document are controlled editable fields."
    - "Only the targeted document changes and all stable identities survive edits."
    - "One Edited badge in the panel-level status row represents query/document divergence; no per-field changed badges are present."
    - "No add/remove control or fixed-document-count branch exists."
    - "resetConfirmed restores the complete selected snapshot, including vectors and setup, in unit tests."
    - "HTML-shaped user input remains visible literal text and the production build passes."
  </acceptance_criteria>
  <done>A student can select and edit all local source material through one immutable reducer session, with stable document identities and a trustworthy derived Edited state.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | Important symbols or contract |
|---|---|
| `src/content/scenarios.ts` | `scenarios`, `getScenarioById`, five canonical immutable scenario records |
| `src/content/lessonSteps.ts` | `lessonSteps`, `firstLessonStepId`, eleven stable `StepId` records |
| `src/domain/simulation.ts` | `SearchDocument`, `Vector2D`, `ScenarioVectors`, `Scenario`, `StepId`, `LessonStep`, `SimulationSession`, `SimulationAction`, `buildSessionFromScenario`, `simulationReducer`, derived selectors |
| `src/domain/simulation.test.ts` | Immutability, switching, editing, reset, document-count independence, and bounded navigation tests |
| `src/features/input-panel/InputPanel.tsx` | `InputPanel`, scenario selector, query editor, numbered document cards, panel-level Edited status row |
| `src/features/input-panel/InputPanel.test.tsx` | SCEN-01, SCEN-02, SCEN-03, D-05, D-06, D-13, D-15, D-16, D-17 interaction coverage |
| `src/App.tsx` | `useReducer` session ownership and live announcements |
| `src/App.test.tsx` | Cross-slice scenario selection and Start Search regression coverage |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| immutable content to editable session | Trusted shipped defaults are copied into mutable browser-session state. |
| user input to React rendering | Untrusted classroom text crosses from textareas into state and visible UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering / Elevation | InputPanel and App text rendering | mitigate | Controlled textarea values and React text nodes only; test literal HTML-shaped input and forbid `dangerouslySetInnerHTML`. |
| T-01-02 | Tampering | Existing package graph | mitigate | Do not add packages in this plan; retain the audited lockfile from Plan 01-01. |
| T-01-03 | Denial of Service | Reset confirmation | transfer to Plan 01-03 | Reducer reset is pure and tested here; accessible modal behavior is implemented in Plan 01-03. |
| T-01-SC | Tampering | npm supply chain | mitigate | No new install; preserve package-lock and include all identities in the end-of-phase review. |
</threat_model>

<verification>
- `npm run test -- --run src/domain/simulation.test.ts`
- `npm run test -- --run src/features/input-panel/InputPanel.test.tsx src/App.test.tsx`
- `npm run build`
- Confirm `rg -n "dangerouslySetInnerHTML|innerHTML|documents\\[[0-9]+\\]|\\.length === 7|add document|remove document" src` has no prohibited implementation match.
</verification>

<success_criteria>
- SCEN-01: five complete built-in scenarios can be selected.
- SCEN-02: the active query is editable through canonical reducer state.
- SCEN-03: every active document is editable by stable ID.
- D-05, D-06, D-07, D-13, D-15, D-16, and D-17 are covered by automated tests and visible behavior.
- All scenario data and state changes remain local to the static browser bundle.
</success_criteria>

<output>
Create `.planning/phases/01-guided-classroom-shell/01-02-SUMMARY.md` when done.
</output>

---
phase: 01-guided-classroom-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - eslint.config.js
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - components.json
  - THIRD_PARTY_NOTICES.md
  - src/main.tsx
  - src/App.tsx
  - src/App.test.tsx
  - src/index.css
  - src/test/setup.ts
  - src/lib/utils.ts
  - src/assets/fonts/AtkinsonHyperlegible-Regular.woff2
  - src/assets/fonts/AtkinsonHyperlegible-Bold.woff2
  - src/assets/fonts/OFL.txt
  - src/components/ui/button.tsx
  - src/components/ui/select.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/label.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/ui/separator.tsx
autonomous: true
requirements:
  - QUAL-02
  - QUAL-06
must_haves:
  truths:
    - "D-01 and D-02: A student can load one static page whose left, center, and widest right regions establish the approved classroom hierarchy."
    - "D-03: The walking skeleton uses the browser document scroll and introduces no independently scrolling panel."
    - "D-04: The page uses the exact bright, projector-safe visual tokens and restrained accent rules from 01-UI-SPEC.md."
    - "The student can activate Start Search and see the first registered-step preview, proving a real browser interaction in the static bundle."
    - "The application builds to dist without API, database, authentication, environment secrets, or server-runtime dependencies."
  artifacts:
    - path: "package.json"
      provides: "Pinned static SPA, lint, and Vitest scripts"
      contains: "\"build\""
    - path: "vite.config.ts"
      provides: "React, Tailwind, alias, and jsdom test configuration"
      contains: "tailwindcss"
    - path: "components.json"
      provides: "Vite-compatible shadcn/ui preset contract"
      contains: "\"rsc\": false"
    - path: "src/App.tsx"
      provides: "Loadable classroom frame and Start Search interaction"
      exports: ["default"]
    - path: "src/index.css"
      provides: "Exact projector-safe tokens, typography, focus, and grid rules"
      contains: "#087F8C"
    - path: "src/App.test.tsx"
      provides: "Walking-skeleton happy-path component test"
      contains: "Start Search"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React root render"
      pattern: "createRoot.*App"
    - from: "vite.config.ts"
      to: "src/index.css"
      via: "Tailwind Vite plugin and CSS import"
      pattern: "tailwindcss"
    - from: "src/App.test.tsx"
      to: "src/App.tsx"
      via: "Testing Library render and Start Search click"
      pattern: "render\\(<App"
---

<objective>
Establish the tested static React walking skeleton and the exact classroom visual system required by D-01 through D-04.

Purpose: Prove the real frontend-only stack, build path, design tokens, approved component registry, and one working classroom interaction before domain state expands.
Output: A Vite React TypeScript SPA, Vitest harness, official shadcn/ui primitives, locally bundled Atkinson Hyperlegible fonts, projector-safe three-region frame, and passing static build.
</objective>

## Phase Goal

**As a** high school student, **I want to** open a scenario-driven search lesson, edit its source material, navigate its steps, and reset safely, **so that** I can inspect how a search engine processes a query in class.

<execution_context>
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/workflows/execute-plan.md
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-guided-classroom-shell/01-CONTEXT.md
@.planning/phases/01-guided-classroom-shell/01-RESEARCH.md
@.planning/phases/01-guided-classroom-shell/01-VALIDATION.md
@.planning/phases/01-guided-classroom-shell/01-PATTERNS.md
@.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md
@.planning/phases/01-guided-classroom-shell/SKELETON.md
@docs/PRD.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the audited static stack and write the failing walking-skeleton test</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-RESEARCH.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-VALIDATION.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md</file>
    <file>.planning/phases/01-guided-classroom-shell/SKELETON.md</file>
  </read_first>
  <files>package.json, package-lock.json, index.html, eslint.config.js, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, components.json, src/main.tsx, src/App.tsx, src/App.test.tsx, src/test/setup.ts, src/lib/utils.ts, src/components/ui/button.tsx, src/components/ui/select.tsx, src/components/ui/textarea.tsx, src/components/ui/label.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/ui/progress.tsx, src/components/ui/alert-dialog.tsx, src/components/ui/separator.tsx</files>
  <behavior>
    - "RED: rendering App is expected to expose Search Engine Lab, the purpose sentence, three named regions, and Start Search."
    - "RED: activating Start Search is expected to replace the setup workspace copy with Tokenization is ready."
    - "The test must fail against the untouched Vite template because the classroom shell does not yet exist."
  </behavior>
  <action>Before package installation, query npm metadata for every audited package and for the shadcn CLI, record the resolved name, version, repository URL, and absence or presence of postinstall scripts in the task log, and stop if an identity does not match the official repositories listed in 01-RESEARCH.md or shadcn-ui/ui. Create the official React TypeScript Vite scaffold in a temporary child directory because the repository root is non-empty, then copy only the scaffold files into the root without altering .planning, docs, AGENTS.md, or existing user files. Pin the audited versions from the Package Legitimacy Audit, add Vitest, React Testing Library, user-event, and jsdom, configure non-watch test scripts, add the @ alias, React and Tailwind Vite plugins, and jsdom setup. Initialize shadcn/ui with preset b1sAmVzcG in Vite mode, require components.json to use rsc false, TSX, CSS variables, Lucide, and @ aliases, and add only the nine official components listed by the UI-SPEC. Do not introduce Next.js, routing, server actions, API code, environment secrets, Playwright, or a third-party registry. Remove Vite demo assets and write src/App.test.tsx first so the asserted classroom interaction is demonstrably red.</action>
  <verify>
    <automated>$result = npm run test -- --run src/App.test.tsx 2>&1; if ($LASTEXITCODE -eq 0) { throw "Expected RED test to fail before classroom shell implementation" }; if (($result | Out-String) -notmatch "Search Engine Lab|Tokenization is ready") { throw "Failure did not come from the intended walking-skeleton expectation" }</automated>
  </verify>
  <acceptance_criteria>
    - "The root contains a Vite React TypeScript project while all pre-existing planning and product files remain intact."
    - "Package metadata checks match the official repositories; no SLOP package or unexpected postinstall script is accepted."
    - "The configured test command is non-watch and the new App test fails only because the classroom UI has not been implemented."
    - "components.json and generated UI primitives match the exact Vite-compatible official-registry contract."
  </acceptance_criteria>
  <done>The static toolchain and approved UI registry are installed, the intended happy-path test is red, and no backend-capable dependency or file structure exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the projector-safe classroom frame and make the static interaction pass</name>
  <read_first>
    <file>.planning/phases/01-guided-classroom-shell/01-CONTEXT.md</file>
    <file>.planning/phases/01-guided-classroom-shell/01-UI-SPEC.md</file>
    <file>src/App.test.tsx</file>
    <file>components.json</file>
    <file>src/index.css</file>
  </read_first>
  <files>THIRD_PARTY_NOTICES.md, src/App.tsx, src/App.test.tsx, src/index.css, src/assets/fonts/AtkinsonHyperlegible-Regular.woff2, src/assets/fonts/AtkinsonHyperlegible-Bold.woff2, src/assets/fonts/OFL.txt</files>
  <behavior>
    - "GREEN: the page renders Search Engine Lab and See how a query becomes a ranked list, one step at a time."
    - "GREEN: the named Search inputs, Lesson steps, and Visualization regions render left-to-right with Visualization widest per D-01 and D-02."
    - "GREEN: Start Search changes the current registered-step preview to Tokenization is ready without a network request."
    - "The supported layout has one page scroll, visible focus, and no independently scrolling panel per D-03."
  </behavior>
  <action>Implement the real static walking-skeleton interaction in App using React client state: setup shows the exact empty-state heading and body, and Start Search changes the lesson and visualization preview to Tokenization using the exact UI-SPEC copy without claiming calculations. Apply D-01 through D-04 exactly: 1280px-plus projector target, 28/24/48 grid with 16px gap, 24px page padding, 1600px maximum width, white bordered panels, right-panel visual emphasis, whole-page scrolling, and no equal-height forcing. In index.css import Tailwind 4, define the exact colors, four type sizes, two weights, spacing, 44px controls, 4px focus ring and offset, and reduced-motion behavior. Bundle Atkinson Hyperlegible regular and bold WOFF2 files from the official Google Fonts/Braille Institute source with its license and record the source and license in THIRD_PARTY_NOTICES.md. Use semantic header, main, section, and accessible region names. Use official shadcn Button, Card, Badge, and Separator where applicable; icons may only accompany visible text. Do not add responsive stacking, dark mode, gradients, panel scroll containers, search calculations, or user-editable fields in this scaffold slice.</action>
  <verify>
    <automated>npm run test -- --run src/App.test.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - "The walking-skeleton test passes after the Start Search interaction is implemented."
    - "The CSS contains the exact #F5F7F4, #FFFFFF, #087F8C, #17212B, #465766, #AAB7BE, #E7EEF0, #B42318, and #FDECEA tokens."
    - "The page uses Atkinson Hyperlegible weights 400 and 700 from local assets and no runtime font request."
    - "At 1280px or wider, Visualization is the widest region and the document body is the only page-level vertical scroll container."
    - "npm run build emits dist successfully and source/config scans contain no API, database, auth, server, or dangerouslySetInnerHTML implementation."
  </acceptance_criteria>
  <done>A student can load and operate the first static classroom interaction in the exact visual frame, and the tested Vite production bundle is backend-free.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | Important symbols or contract |
|---|---|
| `package.json` | `dev`, `build`, `lint`, `test`, `preview` scripts and pinned audited dependencies |
| `package-lock.json` | Reproducible official npm dependency graph |
| `vite.config.ts` | `react()`, `tailwindcss()`, `@` alias, Vitest jsdom configuration |
| `components.json` | shadcn preset `b1sAmVzcG`, `rsc: false`, CSS variables, Lucide, `@/` aliases |
| `src/main.tsx` | Static SPA mount |
| `src/App.tsx` | `App`, setup-to-Tokenization walking interaction |
| `src/App.test.tsx` | First user-facing happy-path contract |
| `src/index.css` | UI-SPEC tokens, typography, focus, motion, and D-01 to D-04 grid |
| `src/test/setup.ts` | Shared Vitest DOM cleanup/setup |
| `src/lib/utils.ts` | shadcn class-name utility |
| `src/components/ui/*.tsx` | Approved Button, Select, Textarea, Label, Card, Badge, Progress, AlertDialog, Separator primitives |
| `src/assets/fonts/*` | Locally bundled Atkinson Hyperlegible regular/bold assets and license |
| `THIRD_PARTY_NOTICES.md` | Font source and license attribution |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry to local build | External package metadata and package contents enter the trusted project toolchain. |
| static bundle to browser | Generated assets execute in the classroom browser without a server-side control plane. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering / Elevation | Future editable text rendering | mitigate | Establish a React text-node-only rule and forbid `dangerouslySetInnerHTML`; Plan 01-02 and 01-03 test edited text visibly. |
| T-01-02 | Tampering | npm and shadcn installs | mitigate | Verify package identity, repository, version, and postinstall metadata before install; use only audited npm packages and the official shadcn registry. |
| T-01-03 | Denial of Service | Reset modal | transfer to Plan 01-03 | The final AlertDialog focus and escape behavior is implemented and tested in Plan 01-03. |
| T-01-SC | Tampering | npm package supply chain | mitigate | Keep the research audit, package-lock, automated metadata checks, and consolidated end-of-phase human identity review. |
</threat_model>

<verification>
- `npm run test -- --run src/App.test.tsx`
- `npm run build`
- Confirm `dist/index.html` references only static assets.
- Confirm `rg -n "dangerouslySetInnerHTML|fetch\\(|axios|express|next/|prisma|supabase|firebase" src package.json` has no unapproved runtime match.
</verification>

<success_criteria>
- QUAL-06: the application loads and builds as a static browser site with no backend.
- QUAL-02 foundation: exact projector-safe visual tokens, typography, region hierarchy, and focus treatment are present.
- D-01 through D-04 are visible in the rendered walking skeleton.
- The first user interaction is covered by a passing automated test and production build.
</success_criteria>

<output>
Create `.planning/phases/01-guided-classroom-shell/01-01-SUMMARY.md` when done.
</output>

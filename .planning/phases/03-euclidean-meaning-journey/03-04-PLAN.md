---
phase: 03-euclidean-meaning-journey
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/features/visualization-panel/semantic-steps/SemanticVisualizationSteps.tsx
  - src/features/visualization-panel/SemanticVisualizationSteps.test.tsx
autonomous: true
requirements:
  - SEMA-03
user_setup: []
must_haves:
  truths:
    - "SEMA-03/ZOOM: SVG Meaning Map allows mouse dragging to pan, wheel scrolling to zoom, and provides a keyboard-accessible floating panel for Zoom In (+), Zoom Out (-), and Reset view (↺)."
  artifacts:
    - path: "src/features/visualization-panel/semantic-steps/SemanticVisualizationSteps.tsx"
      provides: "Zoom and pan state, SVG transform group, and interactive controls overlay"
    - path: "src/features/visualization-panel/SemanticVisualizationSteps.test.tsx"
      provides: "Component tests verifying scale and translation changes on mouse events and controls"
---

<objective>
Enhance the SVG Meaning Map with interactive zoom, pan, and floating accessibility controls to improve legibility of clustered vectors.

Purpose: Solve classroom presentation and grouping legibility issues on desktop viewports by letting students explore clustered coordinates manually.
Output: State-driven scale/translation transformation group inside MeaningMap and clickable floating overlay controls.
</objective>

<execution_context>
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@src/features/visualization-panel/semantic-steps/SemanticVisualizationSteps.tsx
@src/features/visualization-panel/SemanticVisualizationSteps.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit and integration tests for zoom, pan, and reset controls</name>
  <files>src/features/visualization-panel/SemanticVisualizationSteps.test.tsx</files>
  <read_first>
    @src/features/visualization-panel/SemanticVisualizationSteps.test.tsx
  </read_first>
  <behavior>
    - Test that zoom in and zoom out buttons adjust the transformation group scale.
    - Test that reset view button resets the transform scale to 1.0 and pan translation to 0,0.
    - Test that drag events update the transform translate values.
  </behavior>
  <action>Add failing assertions in `SemanticVisualizationSteps.test.tsx` verifying click events on `+`, `-`, and `Reset` buttons modify the transform property of the inner SVG group. Also simulate mouse events for dragging to test panning.</action>
  <verify>
    <automated>npm test -- src/features/visualization-panel/SemanticVisualizationSteps.test.tsx -t "zoom"</automated>
  </verify>
  <acceptance_criteria>
    - Zoom/pan test suite passes and verifies interactive behavior.
  </acceptance_criteria>
  <done>Interactive zoom/pan tests exist and verify state changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement zoom and pan state with transform group and floating controls</name>
  <files>src/features/visualization-panel/semantic-steps/SemanticVisualizationSteps.tsx</files>
  <read_first>
    @src/features/visualization-panel/semantic-steps/SemanticVisualizationSteps.tsx
  </read_first>
  <behavior>
    - SVG inner contents are enclosed in `<g transform="translate(pan.x, pan.y) scale(zoom)">`.
    - Floating panel overlay with interactive zoom buttons is rendered inside a relative container wrapping the SVG.
    - Mouse drag and mouse wheel events update pan/zoom coordinates.
  </behavior>
  <action>Implement the React mouse/wheel event handlers, coordinate boundary checks, transform group, and the control button overlay inside `SemanticVisualizationSteps.tsx`.</action>
  <verify>
    <automated>npm test -- src/features/visualization-panel/SemanticVisualizationSteps.test.tsx</automated>
    <automated>npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass, and SVG zooming/panning works seamlessly.
  </acceptance_criteria>
  <done>MeaningMap features interactive zoom, pan, and control overlays.</done>
</task>

</tasks>

<threat_model>
- T-03-04 (XSS): Ensure all document labels and mouse/touch coords are handled securely as numeric/literal variables without executing arbitrary HTML or JavaScript.
</threat_model>

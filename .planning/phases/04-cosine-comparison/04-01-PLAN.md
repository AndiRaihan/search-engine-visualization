---
phase: 04-cosine-comparison
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/simulation.ts
  - src/domain/simulation.test.ts
autonomous: true
requirements:
  - SEMA-06
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - "SEMA-06/D-07: Cosine similarity is calculated as a pure finite number for every query/document vector pair, returning 0 when either vector has zero magnitude."
    - "SEMA-06: Cosine semantic ranking sorts by raw highest similarity and uses original document index as the deterministic tie-breaker."
    - "QUAL-04: Unit tests cover tokenization, TF, document frequency, IDF, TF-IDF, Euclidean distance, cosine similarity, and deterministic keyword and semantic rankings."
    - "D-06: Domain data exposes enough cosine breakdown fields for the UI to show formula, dot product, query length, document length, and final similarity."
    - "D-08: Ranked semantic rows carry metric-specific evidence text so UI explanations can cite Distance or Similarity from one source of truth."
  artifacts:
    - path: "src/domain/simulation.ts"
      provides: "Pure cosine helper, cosine ranker, cosine breakdown builder, and metric-ready semantic row evidence"
      exports: ["cosineSimilarity", "rankByCosineSimilarity", "buildCosineBreakdown"]
    - path: "src/domain/simulation.test.ts"
      provides: "Unit tests for cosine helper, zero-vector guard, descending ranking, deterministic ties, and full domain-engine coverage"
  key_links:
    - from: "src/domain/simulation.ts"
      to: "src/domain/simulation.test.ts"
      via: "exported pure helpers tested directly"
      pattern: "cosineSimilarity|rankByCosineSimilarity|buildCosineBreakdown"
    - from: "rankByCosineSimilarity"
      to: "buildSemanticSnapshot"
      via: "raw score semantics kept in domain layer for Plan 04-02 UI consumption"
      pattern: "similarity"
---

<objective>
Implement cosine similarity, stable cosine ranking, and complete domain-engine unit coverage for the semantic model.

Purpose: Students must be able to trust the cosine values before the UI lets them switch ranking modes.
Output: Pure cosine math helpers, deterministic cosine ranking, cosine breakdown data, and focused unit tests covering the full search engine calculation surface.
</objective>

<execution_context>
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/workflows/execute-plan.md
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-cosine-comparison/04-CONTEXT.md
@.planning/phases/04-cosine-comparison/04-RESEARCH.md
@.planning/phases/04-cosine-comparison/04-PATTERNS.md
@.planning/phases/04-cosine-comparison/04-VALIDATION.md
@.planning/phases/03-euclidean-meaning-journey/03-03-SUMMARY.md
@src/domain/simulation.ts
@src/domain/simulation.test.ts
</context>

## Artifacts This Phase Produces

- `src/domain/simulation.ts`: `cosineSimilarity`, `rankByCosineSimilarity`, `buildCosineBreakdown`, metric-aware semantic evidence fields, and finite numeric guards.
- `src/domain/simulation.test.ts`: focused tests named with `cosine`, `zero vector`, `deterministic ranking`, and `domain engine coverage`.
- No package installs, backend work, persistence, or schema changes.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock cosine math and ranking tests</name>
  <files>src/domain/simulation.test.ts</files>
  <read_first>
    @.planning/phases/04-cosine-comparison/04-CONTEXT.md
    @.planning/phases/04-cosine-comparison/04-RESEARCH.md
    @.planning/phases/04-cosine-comparison/04-VALIDATION.md
    @src/domain/simulation.test.ts
    @src/domain/simulation.ts
  </read_first>
  <behavior>
    - Test: `cosine` computes dot product divided by vector magnitudes and formats visible values to three decimals.
    - Test: `zero vector` verifies D-07 by returning finite raw similarity 0 and display string `0.000` when query or document magnitude is zero.
    - Test: `deterministic ranking` verifies highest raw cosine similarity sorts first and equal raw scores use originalIndex order.
    - Test: `domain engine coverage` verifies existing tokenization, TF, document frequency, IDF, TF-IDF, Euclidean distance, cosine similarity, and deterministic ranking coverage remains present for QUAL-04.
    - Test: `cosine breakdown` verifies D-06 fields expose formula, dot product substitution/evaluation, query length, document length, denominator, and final similarity.
  </behavior>
  <action>Add failing unit tests to `src/domain/simulation.test.ts` before changing production code. Import the planned public helpers by name so the RED run fails for missing exports or missing behavior. Keep tests on pure functions only; do not render React, mutate reducer state, or compute cosine values in JSX. Use raw-number assertions for ordering and `formatThreeDecimals` assertions for display values so sorting never depends on rounded strings. Cover SEMA-06, QUAL-04, D-06, D-07, and the D-08 data contract.</action>
  <verify>
    <automated>npm test -- --run src/domain/simulation.test.ts -t "cosine"</automated>
    <automated>npm test -- --run src/domain/simulation.test.ts -t "zero vector|deterministic ranking|domain engine coverage"</automated>
  </verify>
  <acceptance_criteria>
    - Tests encode cosine similarity, zero-vector guard, descending ranking, deterministic ties, and breakdown fields before production code is implemented.
    - Tests fail in the RED state for missing cosine exports or incorrect behavior.
    - Existing keyword and Euclidean tests remain in the same file and are not weakened.
  </acceptance_criteria>
  <done>RED tests define SEMA-06, QUAL-04, D-06, D-07, and D-08 domain behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure cosine helpers and metric evidence</name>
  <files>src/domain/simulation.ts, src/domain/simulation.test.ts</files>
  <read_first>
    @.planning/phases/04-cosine-comparison/04-PATTERNS.md
    @.planning/phases/04-cosine-comparison/04-RESEARCH.md
    @src/domain/simulation.ts
    @src/domain/simulation.test.ts
  </read_first>
  <behavior>
    - `cosineSimilarity(query, doc)` returns a finite raw number in [-1, 1] for nonzero vectors and 0 for any zero-magnitude vector per D-07.
    - `rankByCosineSimilarity` sorts by highest raw similarity first and breaks ties by originalIndex.
    - `buildCosineBreakdown` exposes D-06 formula, dot product, query length, document length, denominator, and final similarity as formatted strings derived from raw vector values.
    - Existing Euclidean helpers, Euclidean rankings, keyword snapshot behavior, and semantic missed-document behavior remain unchanged.
  </behavior>
  <action>Implement the pure cosine helpers in `src/domain/simulation.ts` next to the existing Euclidean helpers. Add exported types for cosine breakdown data and metric evidence only where they remove duplication for Plan 04-02. Reuse `Vector2D`, `formatTwoDecimals`, and `formatThreeDecimals`; guard division before formatting so no caller receives `NaN` or `Infinity`. Mirror the existing Euclidean ranker structure, but compare similarities descending and use originalIndex for exact or near ties. If adding metric labels or explanation fields to semantic ranked rows, keep the default Euclidean behavior byte-for-byte equivalent from a user perspective and derive all text from domain data. Do not add dependencies, reducers, UI controls, or SVG behavior in this plan.</action>
  <verify>
    <automated>npm test -- --run src/domain/simulation.test.ts -t "cosine"</automated>
    <automated>npm test -- --run src/domain/simulation.test.ts -t "semantic|euclidean|ranking"</automated>
    <automated>npm test -- --run src/domain/simulation.test.ts</automated>
    <automated>npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - Cosine helper returns exact finite values for normal and zero vectors.
    - Cosine ranking uses raw values, highest-first ordering, and stable tie handling.
    - Breakdown strings contain the D-06 calculation pieces needed by the UI.
    - Existing tokenization, TF, DF, IDF, TF-IDF, Euclidean distance, keyword ranking, and semantic ranking tests pass.
  </acceptance_criteria>
  <done>Domain engine exposes tested cosine similarity, deterministic cosine ranking, and breakdown data without changing the existing Euclidean classroom flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scenario vectors -> domain math | Curated local coordinates become ranking evidence that students trust. |
| Domain snapshot -> UI explanation | Numeric values and text labels feed visual ranking surfaces in Plan 04-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | `cosineSimilarity` | mitigate | Check query and document magnitudes before division and return finite 0 for zero vectors per D-07. |
| T-04-02 | Spoofing | `rankByCosineSimilarity` | mitigate | Sort on raw similarity values only, then originalIndex ties; unit tests include rounded-value tie risks. |
| T-04-03 | Tampering | `buildCosineBreakdown` | mitigate | Generate equations from numeric vector fields, not user-editable document text. |
| T-04-SC | Tampering | npm package supply chain | mitigate | No package-manager install tasks are part of this plan; use existing TypeScript and Vitest only. |
</threat_model>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 4 | Users can switch between Euclidean distance and cosine similarity and understand selected ranking evidence. | 04-01, 04-02 | COVERED | 04-01 builds trusted math; 04-02 exposes switching and explanations. |
| REQ | SEMA-06 | Inspect cosine similarity for every document and rank highest similarity first. | 04-01, 04-02 | COVERED | Domain values/ranking here, UI display in 04-02. |
| REQ | SEMA-07 | Toggle semantic ranking between Euclidean and cosine. | 04-02 | COVERED | Reducer-backed toggle and panel rendering. |
| REQ | SEMA-08 | Semantic explanation generated from selected displayed metric and curated vectors. | 04-01, 04-02 | COVERED | Domain evidence here, UI explanation in 04-02. |
| REQ | QUAL-04 | Unit tests cover tokenization, TF, DF, IDF, TF-IDF, Euclidean distance, cosine similarity, and rankings. | 04-01 | COVERED | Complete domain unit gate in this plan. |
| RESEARCH | Centralized metric-aware semantic snapshot | Keep metric math and rank evidence in `src/domain/simulation.ts`. | 04-01, 04-02 | COVERED | Helpers and evidence here; snapshot integration in 04-02. |
| RESEARCH | Deterministic raw-value sorting | Do not sort on formatted strings. | 04-01 | COVERED | Unit tests force raw cosine ordering and ties. |
| RESEARCH | Zero-vector guard | Avoid `NaN` or `Infinity` from cosine denominators. | 04-01 | COVERED | D-07 helper test and implementation. |
| RESEARCH | No new packages | Reuse React, TypeScript, native SVG, and Vitest. | 04-01, 04-02 | COVERED | No install tasks. |
| CONTEXT | D-01 | Toggle in Visualization Panel top-right on semantic-ranking and final-comparison. | 04-02 | COVERED | UI plan owns placement. |
| CONTEXT | D-02 | `semanticMetric` in reducer state with default Euclidean and `metricToggled` action. | 04-02 | COVERED | UI/state plan owns reducer integration. |
| CONTEXT | D-03 | Hide direct query-to-document Euclidean lines in cosine mode. | 04-02 | COVERED | MeaningMap overlay switch. |
| CONTEXT | D-04 | Draw dashed origin rays to query and documents in cosine mode. | 04-02 | COVERED | MeaningMap overlay switch. |
| CONTEXT | D-05 | Preserve zoom, pan, and coordinate table across both modes. | 04-02 | COVERED | Tests protect Phase 3 behavior. |
| CONTEXT | D-06 | Show tabular cosine formula, dot product, vector lengths, and final similarity. | 04-01, 04-02 | COVERED | Domain breakdown data here, panel in 04-02. |
| CONTEXT | D-07 | Return 0.000 when either cosine vector magnitude is zero. | 04-01 | COVERED | Helper guard and tests. |
| CONTEXT | D-08 | Ranking explanations cite active metric and ordering rule. | 04-01, 04-02 | COVERED | Domain evidence here, UI copy in 04-02. |
</source_audit>

<verification>
- Run `npm test -- --run src/domain/simulation.test.ts -t "cosine"`.
- Run `npm test -- --run src/domain/simulation.test.ts -t "zero vector|deterministic ranking|domain engine coverage"`.
- Run `npm test -- --run src/domain/simulation.test.ts`.
- Run `npm test -- --run` as the plan gate.
</verification>

<success_criteria>
- SEMA-06 domain behavior is satisfied by tested cosine similarity values and highest-similarity ranking.
- QUAL-04 is satisfied by unit coverage for tokenization, TF, document frequency, IDF, TF-IDF, Euclidean distance, cosine similarity, and deterministic ranking functions.
- D-06 and D-07 are represented in pure domain helpers.
- Existing Euclidean behavior and Phase 3 semantic ranking tests remain green.
</success_criteria>

<output>
Create `.planning/phases/04-cosine-comparison/04-01-SUMMARY.md` when done.
</output>

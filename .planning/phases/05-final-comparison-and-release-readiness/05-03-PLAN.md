---
phase: 05-final-comparison-and-release-readiness
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - tests/smoke/search-flow.spec.ts
autonomous: false
requirements:
  - FLOW-05
  - FLOW-06
  - COMP-01
  - COMP-02
  - COMP-03
  - QUAL-01
  - QUAL-03
  - QUAL-05
requirements_addressed:
  - FLOW-05
  - FLOW-06
  - COMP-01
  - COMP-02
  - COMP-03
  - QUAL-01
  - QUAL-03
  - QUAL-05
user_setup: []
must_haves:
  truths:
    - "QUAL-05: Browser smoke tests verify app loading, scenario selection, query editing, Previous/Next navigation by button and documented keyboard shortcuts, reset, metric switching, Run All, and final comparison."
    - "D-01/D-02/D-03: Browser smoke coverage confirms normal-motion Run All starts the guided sequence and can be cancelled by student interaction, while reduced-motion Run All reaches final comparison immediately."
    - "The browser smoke suite runs against the built Vite static preview artifact, not a backend service."
    - "FLOW-05/FLOW-06/COMP-01/COMP-02/COMP-03/QUAL-01/QUAL-03 are covered by browser-level smoke assertions after 05-01 and 05-02."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Chromium smoke-test config with static Vite build/preview webServer"
    - path: "tests/smoke/search-flow.spec.ts"
      provides: "Critical classroom flow browser smoke tests"
    - path: "package.json"
      provides: "Smoke test script and Playwright dev dependency"
  key_links:
    - from: "tests/smoke/search-flow.spec.ts"
      to: "playwright.config.ts"
      via: "baseURL served by Vite preview webServer"
      pattern: "baseURL|webServer"
    - from: "package.json"
      to: "playwright.config.ts"
      via: "npm run test:smoke invokes playwright test"
      pattern: "test:smoke"
---

<objective>
Add browser-level smoke coverage and a production static build gate for the completed classroom flow.

Purpose: The final release should prove the integrated app works in a real browser and still ships as a static site.
Output: Package-gated Playwright setup, critical-flow smoke tests, and release verification commands.
</objective>

<execution_context>
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/workflows/execute-plan.md
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-final-comparison-and-release-readiness/05-RESEARCH.md
@.planning/phases/05-final-comparison-and-release-readiness/05-01-SUMMARY.md
@.planning/phases/05-final-comparison-and-release-readiness/05-02-SUMMARY.md
@package.json
@package-lock.json
@vite.config.ts
@src/App.tsx
@src/App.test.tsx
</context>

## Artifacts This Plan Produces

- `@playwright/test` dev dependency and lockfile update after a blocking package legitimacy checkpoint.
- `playwright.config.ts` using Playwright `webServer` to build and preview the static Vite artifact before tests.
- `tests/smoke/search-flow.spec.ts` covering the Phase 5 critical browser flow.
- `package.json` scripts such as `test:smoke` for the smoke gate.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Playwright package legitimacy</name>
  <what-built>No code is built before this gate. This is the required package legitimacy checkpoint because `05-RESEARCH.md` recommends Playwright but does not include a Package Legitimacy Audit table.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@playwright/test and confirm the package name is exactly `@playwright/test`.
    2. Confirm the package is published by the official Playwright/Microsoft maintainers and links to the official Playwright project.
    3. Open https://playwright.dev/docs/intro and confirm Playwright Test is the official browser testing tool referenced by the docs.
    4. If the package identity is not clearly official, stop and route back to research for a Package Legitimacy Audit.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the executor to install `@playwright/test`, or describe the package concern.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure Playwright and add critical classroom-flow smoke tests</name>
  <files>package.json, package-lock.json, playwright.config.ts, tests/smoke/search-flow.spec.ts</files>
  <read_first>
    @package.json
    @vite.config.ts
    @src/App.tsx
    @src/App.test.tsx
    @.planning/phases/05-final-comparison-and-release-readiness/05-01-SUMMARY.md
    @.planning/phases/05-final-comparison-and-release-readiness/05-02-SUMMARY.md
    @.planning/phases/04-cosine-comparison/04-UI-SPEC.md
    @.planning/phases/05-final-comparison-and-release-readiness/05-RESEARCH.md
  </read_first>
  <behavior>
    - `npm run test:smoke` invokes Playwright Test.
    - Playwright starts a Vite static preview with `npm run build` followed by `npm run preview` on `127.0.0.1:4173`.
    - The config runs Chromium smoke tests with `baseURL` set to the preview URL and reuses an existing server outside CI.
    - Smoke test: load the app, select a built-in scenario, edit the query, use Next and Previous by button interaction, use documented Previous and Next keyboard shortcuts outside editable fields, reset, switch semantic metric, run all, and assert final-comparison content.
    - Smoke test: normal-motion Run All starts the guided sequence; manually navigating or editing query/document text cancels it and prevents a later automatic advance.
    - Smoke test: reduced-motion browser context can run all to final comparison immediately, while exact ~800 ms pacing remains covered by the fake-timer unit test in 05-02.
    - Smoke assertions cover side-by-side comparison headings, rank movement text, selected semantic metric labels, and evidence explanations.
  </behavior>
  <action>After the package checkpoint is approved, install the official `@playwright/test` dev dependency and update `package-lock.json`. Add `test:smoke` to `package.json` to run `playwright test`. Create `playwright.config.ts` using `defineConfig` from `@playwright/test`, a Chromium project, `testDir: './tests/smoke'`, `use.baseURL: 'http://127.0.0.1:4173'`, and `webServer` configured to run the static production artifact through `npm run build` and `npm run preview -- --host 127.0.0.1 --port 4173`. Create `tests/smoke/search-flow.spec.ts` using Playwright web-first locators. Cover the primary classroom path end to end: load `baseURL`, verify `Search Engine Lab`, select a non-default scenario through the existing scenario control, edit the query textarea, start or run the lesson, exercise Next and Previous controls, exercise documented Previous and Next keyboard shortcuts with focus outside editable fields (`Alt+ArrowLeft` and `Alt+ArrowRight`) and assert the active step changes in both directions, reset the scenario, switch the Phase 4 semantic metric to Cosine, activate Run All by button and by documented keyboard shortcut, and assert `Final Comparison`, `Keyword ranking`, `Semantic ranking`, visible rank movement text, and explanation evidence are present. In a normal-motion test, start Run All, prove the active step begins to advance, then cancel it with a manual navigation action and separately a query or document textarea edit; wait past one scheduled interval and assert the user remains at the manually selected/current step. Add a reduced-motion browser context that reaches final comparison via Run All immediately. Keep the exact ~800 ms cadence assertion in 05-02's fake-timer unit test, rather than making the smoke suite timing-sensitive. Keep this as a static Vite preview; do not add backend services, persistence, authentication, analytics, production search infrastructure, color-only assertions, or implementation-private class-name assertions.</action>
  <verify>
    <automated>npm ls @playwright/test</automated>
    <automated>npm run test:smoke -- --list</automated>
    <automated>npm run test:smoke</automated>
  </verify>
  <done>Playwright is installed through the approved package gate, configured for the built static app, and browser smoke tests cover loading, scenario selection, query editing, navigation, reset, metric switching, Run All, reduced motion, and final comparison.</done>
</task>

<task type="auto">
  <name>Task 3: Run the release readiness gate</name>
  <files>None - verification-only gate over the completed static app</files>
  <action>Run the complete release gate after smoke tests are implemented. If `npm test -- --run`, `npm run test:smoke`, or `npm run build` fails because of Phase 5 code, fix the relevant Phase 5 files in this plan. If the failure shows missing Phase 4 metric behavior, stop and route back to Phase 4 instead of implementing Phase 4 behavior here.</action>
  <verify>
    <automated>npm test -- --run</automated>
    <automated>npm run test:smoke</automated>
    <automated>npm run build</automated>
  </verify>
  <done>All unit/component tests, browser smoke tests, and the production static build pass for the completed lesson.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> dev dependency | A new browser-test package is installed into the project. |
| Browser automation -> local static preview | Playwright drives the built app through a local preview server. |
| Student-edit smoke inputs -> app DOM | Smoke tests type user-like strings into editable fields and verify no navigation breakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | `@playwright/test` install | mitigate | Blocking package legitimacy checkpoint before install because research lacks an audit table. |
| T-05-08 | Denial of Service | Playwright webServer | mitigate | Use local `127.0.0.1` preview with bounded timeout and Chromium-only smoke scope. |
| T-05-09 | Spoofing | Smoke selectors | mitigate | Use accessible names and visible text, not private classes or color-only states. |
| T-05-SC | Tampering | npm package supply chain | mitigate | Human verification of official package identity before install; no third-party registry packages. |
</threat_model>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 5 | Complete lesson, compare rankings, accessible tested static release. | 05-01, 05-02, 05-03 | COVERED | This plan covers browser release confidence. |
| REQ | FLOW-05 | Run All advances through complete sequence. | 05-02, 05-03 | COVERED | Unit test covers cadence; smoke test exercises normal-motion sequence and completion. |
| REQ | FLOW-06 | Keyboard controls documented and operable. | 05-02, 05-03 | COVERED | Smoke test exercises documented Previous, Next, and Run All shortcuts. |
| REQ | COMP-01 | Side-by-side rankings in final step. | 05-01, 05-03 | COVERED | Smoke test asserts final comparison headings. |
| REQ | COMP-02 | Rank changes without color alone. | 05-01, 05-03 | COVERED | Smoke test asserts movement text. |
| REQ | COMP-03 | Explanations cite visible evidence. | 05-01, 05-03 | COVERED | Smoke test asserts explanation evidence. |
| REQ | QUAL-01 | Non-color cues for state and movement. | 05-01, 05-02, 05-03 | COVERED | Browser locators assert text labels. |
| REQ | QUAL-03 | Reduced-motion users can complete the flow. | 05-02, 05-03 | COVERED | Unit and smoke tests use reduced motion. |
| REQ | QUAL-05 | Browser smoke tests verify load, scenario selection, query editing, navigation, reset, metric toggle, and final comparison. | 05-03 | COVERED | This plan creates the smoke suite. |
| RESEARCH | Add Playwright smoke tests | Browser-level verification is missing and required. | 05-03 | COVERED | Package-gated Playwright setup. |
| RESEARCH | Run against built static app | Use build plus preview rather than dev-only server. | 05-03 | COVERED | `webServer` uses Vite preview. |
| RESEARCH | Release gate | Run unit tests, smoke tests, and build. | 05-03 | COVERED | Task 3. |
| CONTEXT | D-01 | Run All is an ~800 ms-per-step autoplay slideshow. | 05-02, 05-03 | COVERED | Fake-timer unit coverage owns exact cadence; smoke coverage validates a real browser sequence. |
| CONTEXT | D-02 | Manual navigation and query/document edits cancel autoplay immediately. | 05-02, 05-03 | COVERED | Unit and smoke tests assert cancellation persists past a later scheduled interval. |
| CONTEXT | D-03 | Reduced motion reaches final comparison immediately. | 05-02, 05-03 | COVERED | Smoke test uses a reduced-motion browser context. |
| CONTEXT | D-04/D-05 | Keyboard shortcut mapping is visible beside controls. | 05-02, 05-03 | COVERED | Smoke test activates the documented shortcuts. |
</source_audit>

<verification>
- Complete the blocking package legitimacy checkpoint before installing `@playwright/test`.
- Run `npm run test:smoke -- --list`.
- Run `npm run test:smoke`.
- Run `npm test -- --run`.
- Run `npm run build`.
</verification>

<success_criteria>
- QUAL-05 is satisfied by browser smoke coverage for loading, scenario selection, query editing, button navigation, documented Previous/Next keyboard shortcuts, reset, metric switching, and final comparison.
- FLOW-05, FLOW-06, COMP-01, COMP-02, COMP-03, QUAL-01, and QUAL-03 have browser-level regression coverage, including documented Previous, Next, and Run All shortcuts, normal-motion Run All cancellation, and reduced-motion instant completion.
- The app still builds as a static Vite site with no backend requirement.
- The package install is gated by human verification because the research artifact lacks a package audit table.
</success_criteria>

<output>
Create `.planning/phases/05-final-comparison-and-release-readiness/05-03-SUMMARY.md` when done.
</output>

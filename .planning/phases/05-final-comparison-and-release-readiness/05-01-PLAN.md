---
phase: 05-final-comparison-and-release-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
prerequisites:
  - ".planning/phases/04-cosine-comparison/04-01-SUMMARY.md"
  - ".planning/phases/04-cosine-comparison/04-02-SUMMARY.md"
  - ".planning/phases/04-cosine-comparison/04-VALIDATION.md"
blocked_by:
  - "Phase 4 execution artifacts are absent locally until the Phase 4 summaries exist and 04-VALIDATION.md plus ROADMAP/STATE show Phase 4 as executed or complete; Task 1 must stop before any Phase 5 edit if they are still missing, draft, pending, or inconsistent."
files_modified:
  - src/domain/simulation.ts
  - src/domain/simulation.test.ts
  - src/content/lessonSteps.ts
  - src/App.tsx
  - src/features/visualization-panel/VisualizationPanel.tsx
  - src/features/visualization-panel/final-comparison/FinalComparisonStep.tsx
  - src/features/visualization-panel/final-comparison/FinalComparisonStep.test.tsx
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - QUAL-01
requirements_addressed:
  - COMP-01
  - COMP-02
  - COMP-03
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - "COMP-01: Users can compare keyword and selected semantic rankings side by side on the final-comparison step."
    - "COMP-02/QUAL-01/D-06/D-07: Every document shows keyword rank, semantic rank, and text-plus-shape movement cues: ▲ Moved up, ▼ Moved down, or – No change."
    - "COMP-03: Explanations cite visible keyword score evidence and the selected semantic metric evidence delivered by Phase 4."
    - "D-09/D-10: Activating a document row selects and highlights that document in both ranking columns, then reveals a comparison card with the keyword TF-IDF contribution breakdown on the left and the active semantic distance/similarity calculation on the right."
    - "Phase 4 dependency: `semanticMetric`, `metricToggled`, cosine ranking, final-comparison toggle wiring, completed Phase 4 summaries, and passing Phase 4 verification tests must exist before Phase 5 wiring begins."
  artifacts:
    - path: "src/domain/simulation.ts"
      provides: "Final comparison selector joining keyword and selected semantic ranked outputs by document id"
      exports: ["FinalComparisonSnapshot", "FinalComparisonRow", "buildFinalComparisonSnapshot"]
    - path: "src/features/visualization-panel/final-comparison/FinalComparisonStep.tsx"
      provides: "Side-by-side ranking comparison, synchronized document selection, rank movement table, and two-column evidence card"
      exports: ["FinalComparisonStep"]
    - path: "src/features/visualization-panel/final-comparison/FinalComparisonStep.test.tsx"
      provides: "Component coverage for comparison lists, synchronized selection, two-column metric-aware evidence, rank movement cues, and non-color semantics"
  key_links:
    - from: "src/App.tsx"
      to: "src/domain/simulation.ts"
      via: "derived buildFinalComparisonSnapshot(keywordSnapshot, semanticSnapshot, semanticMetric)"
      pattern: "buildFinalComparisonSnapshot"
    - from: "src/features/visualization-panel/VisualizationPanel.tsx"
      to: "src/features/visualization-panel/final-comparison/FinalComparisonStep.tsx"
      via: "final-comparison route passes keywordSnapshot, semanticSnapshot, and delivered Phase 4 metric props"
      pattern: "FinalComparisonStep"
---

<objective>
Build the final comparison model and UI so students can inspect keyword and selected semantic rankings side by side with rank movement and evidence-based explanations.

Purpose: This phase closes the classroom lesson by making the ranking change observable without blending scores or hiding the selected semantic metric.
Output: A pure final-comparison selector, routed final-comparison visualization, tests, and metric-aware explanation copy.
</objective>

<execution_context>
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/workflows/execute-plan.md
@C:/Tugas Raihan/Latihan Python/search_engine_visualization/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-final-comparison-and-release-readiness/05-RESEARCH.md
@.planning/phases/04-cosine-comparison/04-CONTEXT.md
@.planning/phases/04-cosine-comparison/04-UI-SPEC.md
@src/domain/simulation.ts
@src/domain/simulation.test.ts
@src/App.tsx
@src/App.test.tsx
@src/content/lessonSteps.ts
@src/features/visualization-panel/VisualizationPanel.tsx
@src/features/visualization-panel/VisualizationPanel.test.tsx
@src/features/visualization-panel/semantic-steps/SemanticVisualizationSteps.tsx
@src/features/visualization-panel/SemanticVisualizationSteps.test.tsx
</context>

## Artifacts This Plan Produces

- `src/domain/simulation.ts`: `FinalComparisonRow`, `FinalComparisonSnapshot`, and `buildFinalComparisonSnapshot` using existing keyword ranking and Phase 4 selected semantic ranking outputs.
- `src/features/visualization-panel/final-comparison/FinalComparisonStep.tsx`: final-comparison component with side-by-side rankings, movement cues, and evidence explanations.
- `src/features/visualization-panel/final-comparison/FinalComparisonStep.test.tsx`: tests for COMP-01, COMP-02, COMP-03, and QUAL-01.
- Updated `VisualizationPanel.tsx`, `App.tsx`, and `lessonSteps.ts` wiring for the real final-comparison route and metric-aware lesson copy.

<tasks>

<task type="auto">
  <name>Task 1: Verify delivered Phase 4 metric API before comparison work</name>
  <files>None - read-only preflight over .planning/phases/04-cosine-comparison, src/domain/simulation.ts, src/App.tsx, src/features/visualization-panel/VisualizationPanel.tsx, src/features/visualization-panel/semantic-steps/SemanticVisualizationSteps.tsx</files>
  <read_first>
    @.planning/phases/04-cosine-comparison/04-01-SUMMARY.md
    @.planning/phases/04-cosine-comparison/04-02-SUMMARY.md
    @.planning/phases/04-cosine-comparison/04-VALIDATION.md
    @.planning/phases/04-cosine-comparison/04-CONTEXT.md
    @.planning/phases/04-cosine-comparison/04-UI-SPEC.md
    @.planning/phases/05-final-comparison-and-release-readiness/05-RESEARCH.md
    @.planning/ROADMAP.md
    @.planning/STATE.md
    @src/domain/simulation.ts
    @src/App.tsx
    @src/features/visualization-panel/VisualizationPanel.tsx
    @src/features/visualization-panel/semantic-steps/SemanticVisualizationSteps.tsx
  </read_first>
  <action>Confirm Phase 4 has shipped before editing Phase 5 files. Require completed Phase 4 summaries at `.planning/phases/04-cosine-comparison/04-01-SUMMARY.md` and `.planning/phases/04-cosine-comparison/04-02-SUMMARY.md`. Require `.planning/phases/04-cosine-comparison/04-VALIDATION.md` to be non-draft and free of pending/open checklist items for the Phase 4 validation rows. Reconcile that evidence against `.planning/ROADMAP.md` and `.planning/STATE.md`: Phase 4 must be marked executed, complete, or otherwise explicitly advanced beyond "Not started" / "Ready to advance to Phase 4" before Phase 5 implementation begins. Optional Phase 4 verification/UAT artifacts may be inspected only if they exist, but they must not appear in `read_first`; if present, they must not report failure, blockers, pending status, or unresolved gaps. The delivered code must expose a global selected semantic metric equivalent to `semanticMetric: 'euclidean' | 'cosine'`, a reducer action equivalent to `metricToggled`, cosine similarity and deterministic cosine ranking helpers, metric-aware semantic snapshot/ranking data, metric-aware labels/explanations, metric toggle persistence, and a metric toggle visible on `semantic-ranking` plus `final-comparison` per Phase 4 D-01 and D-02. Prove those with concrete tests for cosine domain behavior, metric toggle persistence, metric-aware labels/explanations, and final-comparison toggle visibility. If any summary, validation state, roadmap/state completion signal, delivered API, optional verification artifact, or required test evidence is absent or failing, stop this plan immediately and return `CHECKPOINT REACHED` telling the orchestrator to execute or repair Phase 4; do not implement cosine, metric state, or the Phase 4 toggle inside Phase 5 except for narrow prop adaptation to the delivered API.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const required=['.planning/phases/04-cosine-comparison/04-01-SUMMARY.md','.planning/phases/04-cosine-comparison/04-02-SUMMARY.md']; for (const p of required) { if (!fs.existsSync(p)) { console.error('Missing Phase 4 completion summary: '+p); process.exit(1); } }"</automated>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/04-cosine-comparison/04-VALIDATION.md'; if (!fs.existsSync(p)) { console.error('Missing Phase 4 validation artifact: '+p); process.exit(1); } const text=fs.readFileSync(p,'utf8'); if (/status:\\s*draft/i.test(text) || /pending/i.test(text) || /⬜/.test(text) || /\\[ \\]/.test(text)) { console.error('Phase 4 validation is still draft or pending. Execute/repair Phase 4 before Phase 5.'); process.exit(1); }"</automated>
    <automated>node -e "const fs=require('fs'); const roadmap=fs.readFileSync('.planning/ROADMAP.md','utf8'); const state=fs.readFileSync('.planning/STATE.md','utf8'); const phase4Incomplete=/Phase 4[^\\n]*\\|\\s*0\\/2\\s*\\|\\s*Not started/i.test(roadmap) || /Ready to advance to Phase 4/i.test(state); if (phase4Incomplete) { console.error('ROADMAP/STATE still show Phase 4 as incomplete. Execute/repair Phase 4 before Phase 5.'); process.exit(1); }"</automated>
    <automated>node -e "const fs=require('fs'); const candidates=['.planning/phases/04-cosine-comparison/04-VERIFICATION.md','.planning/phases/04-cosine-comparison/04-UAT.md']; for (const p of candidates.filter(fs.existsSync)) { const text=fs.readFileSync(p,'utf8'); if (/\\b(FAIL|FAILED|BLOCKER)\\b/i.test(text) || /\\bpending\\b/i.test(text) || /unresolved gap/i.test(text)) { console.error('Optional Phase 4 verification/UAT artifact is not successful: '+p); process.exit(1); } } console.log('No failing optional Phase 4 verification/UAT artifact detected.');"</automated>
    <automated>npm test -- --run src/domain/simulation.test.ts -t "cosine|semantic.*ranking|ranking.*cosine"</automated>
    <automated>npm test -- --run src/App.test.tsx -t "metric toggle|semantic metric"</automated>
    <automated>npm test -- --run src/features/visualization-panel/SemanticVisualizationSteps.test.tsx -t "cosine breakdown|metric explanation|Similarity|Distance"</automated>
    <automated>npm test -- --run src/features/visualization-panel/VisualizationPanel.test.tsx -t "final-comparison|metric toggle"</automated>
  </verify>
  <done>Phase 4 completion summaries, non-draft validation, roadmap/state completion signals, successful optional verification evidence when present, metric state, selected-metric semantic ranking, metric-aware explanations, metric toggle persistence, and final-comparison toggle surfaces are verified, or execution stops before Phase 5 edits and routes back to Phase 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pure final-comparison selector and tests</name>
  <files>src/domain/simulation.ts, src/domain/simulation.test.ts</files>
  <read_first>
    @src/domain/simulation.ts
    @src/domain/simulation.test.ts
    @.planning/phases/05-final-comparison-and-release-readiness/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test: joining ranked keyword and selected semantic outputs returns one row per document id with stable D-number labels.
    - Test: rank movement uses `keywordRank - semanticRank`, where positive means moved up in semantic ranking, negative means moved down, and zero means no change.
    - Test: rows include keyword explanation text and selected semantic metric evidence, using `Distance` for Euclidean and `Similarity` for cosine from the delivered Phase 4 payload.
    - Test: rows preserve the structured evidence needed by the selected-document card: per-token keyword TF-IDF contributions and the active semantic metric's calculation inputs, label, and result.
    - Test: ties and all-zero keyword scores keep existing deterministic ranking order from the source snapshots; the selector does not re-rank.
  </behavior>
  <action>Add exported comparison types and `buildFinalComparisonSnapshot` to `src/domain/simulation.ts`. The selector must join `keywordSnapshot.rankedDocuments` with `semanticSnapshot.rankedDocuments` by document `id`, preserve existing rank fields instead of sorting again, derive `keywordRank`, `semanticRank`, `rankDelta`, `movementDirection`, `movementLabel`, `keywordScore`, `semanticMetricLabel`, `semanticMetricValue`, `keywordExplanation`, and `semanticExplanation`. It must also preserve or adapt structured `keywordContributionBreakdown` data for the selected document's per-token TF-IDF evidence and `semanticCalculationBreakdown` data for the selected metric's distance/similarity calculation inputs and result; do not reduce either side to a prose-only explanation. Use Phase 4's delivered semantic metric shape; if naming differs, adapt at this boundary while keeping the selected metric authoritative. Add tests in `simulation.test.ts` covering COMP-01, COMP-02, COMP-03, D-10, and QUAL-01 behavior before implementation.</action>
  <verify>
    <automated>npm test -- src/domain/simulation.test.ts -t "final comparison|rank movement|semantic metric evidence"</automated>
  </verify>
  <done>The domain model can produce deterministic final-comparison rows for every document without React-specific logic or new ranking algorithms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route and render the final comparison UI</name>
  <files>src/features/visualization-panel/final-comparison/FinalComparisonStep.tsx, src/features/visualization-panel/final-comparison/FinalComparisonStep.test.tsx, src/features/visualization-panel/VisualizationPanel.tsx, src/App.tsx, src/content/lessonSteps.ts</files>
  <read_first>
    @src/features/visualization-panel/VisualizationPanel.tsx
    @src/features/visualization-panel/semantic-steps/SemanticVisualizationSteps.tsx
    @src/features/visualization-panel/keyword-steps/KeywordScoringSteps.tsx
    @src/App.tsx
    @src/content/lessonSteps.ts
    @.planning/phases/04-cosine-comparison/04-UI-SPEC.md
    @.planning/phases/05-final-comparison-and-release-readiness/05-RESEARCH.md
  </read_first>
  <behavior>
    - Component test: `final-comparison` renders `Keyword ranking` and `Semantic ranking` side by side using the selected Phase 4 metric label.
    - Component test: every document row exposes both ranks and text-first movement such as `Moved up 2`, `Moved down 1`, or `No change`.
    - Component test: explanations cite visible keyword score/contribution evidence and visible selected semantic metric evidence.
    - Component test: movement state is conveyed by text and the exact non-color symbols ▲ (up), ▼ (down), or – (unchanged), in addition to any color.
    - Component test: activating a document in either ranking column highlights that document in both columns and shows a comparison card below the rankings with keyword TF-IDF contributions on the left and the selected semantic distance/similarity calculation on the right.
  </behavior>
  <action>Create `FinalComparisonStep` and route `activeStepId === 'final-comparison'` to it from `VisualizationPanel.tsx`. Compute the comparison snapshot in `App.tsx` with `useMemo` from the current keyword snapshot, semantic snapshot, and delivered Phase 4 metric state, then pass it through the visualization panel. Render two adjacent ranking lists or tables with headings `Keyword ranking` and `Semantic ranking`, followed by a per-document rank-movement table. Use stable `D{n}` labels, rank numbers, score/metric values, and text-first movement labels with the explicit non-color symbols `▲` for moved up, `▼` for moved down, and `–` for unchanged; any lucide icon is supplementary and must not replace those symbols or labels. Manage a selected document id in the final-comparison UI: clicking or keyboard-activating a row in either column must mark the matching row in both columns with an accessible selected state and update one detailed comparison card below the rankings. The card must have explicit left and right sections: the left shows that document's keyword TF-IDF contribution breakdown, and the right shows the active Phase 4 semantic metric's distance or similarity calculation, label, and value. Derive `activeStepDescription` in `App.tsx` from the current active step and `session.semanticMetric`: when `activeStepId === 'final-comparison'`, build the runtime description with the selected metric label and pass that runtime string to `LessonPanel`. Keep `lessonSteps.ts` final-comparison text as neutral fallback copy unless the app already has a lesson-step formatter pattern; if such a formatter pattern already exists, use that pattern from `App.tsx` rather than passing static final-comparison text through unchanged. Keep the UI static-SPA only and do not add backend, persistence, authentication, production ML, or a blended keyword/semantic score.</action>
  <verify>
    <automated>npm test -- src/features/visualization-panel/final-comparison/FinalComparisonStep.test.tsx</automated>
    <automated>npm test -- src/features/visualization-panel/VisualizationPanel.test.tsx -t "final comparison"</automated>
    <automated>npm test -- src/App.test.tsx -t "final comparison"</automated>
    <automated>npm test -- --run</automated>
  </verify>
  <done>Users reaching the final step see a real side-by-side comparison, can identify every document's rank movement without color alone, and can activate any document to inspect synchronized highlights plus a two-column card tied to visible keyword TF-IDF and selected semantic evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Student-edited text -> final comparison UI | Query and document text can contain arbitrary strings and must render as text only. |
| Phase 4 metric API -> Phase 5 comparison | Phase 5 trusts delivered selected-metric rankings and must not silently replace missing Phase 4 behavior. |
| Domain comparison selector -> React tables | Rank movement and explanations must remain synchronized with the pure selector output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `FinalComparisonStep` text rendering | mitigate | Render titles/text/explanations as React text nodes; no `dangerouslySetInnerHTML`; keep existing XSS regressions green. |
| T-05-02 | Spoofing | Rank movement labels | mitigate | Derive ranks and movement from `buildFinalComparisonSnapshot`; component tests assert every document shows both ranks and movement text. |
| T-05-03 | Tampering | Phase 4 metric dependency | mitigate | Task 1 blocks execution if Phase 4 summaries, successful verification evidence, semantic metric state, cosine ranking, metric-aware explanations, metric toggle persistence, or final-comparison toggle visibility tests are missing. |
| T-05-SC | Tampering | npm package supply chain | mitigate | No package-manager install tasks are part of this plan. |
</threat_model>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 5 | Complete lesson, compare keyword and semantic rankings side by side, and rely on accessible release. | 05-01, 05-02, 05-03 | COVERED | This plan covers comparison; later plans cover navigation/accessibility and release tests. |
| REQ | COMP-01 | Side-by-side keyword and selected semantic rankings in final step. | 05-01, 05-03 | COVERED | FinalComparisonStep and smoke coverage. |
| REQ | COMP-02 | Rank in both lists and rank changes without color alone. | 05-01, 05-03 | COVERED | Movement text/icons and tests. |
| REQ | COMP-03 | Deterministic explanations cite visible keyword or selected semantic metric evidence. | 05-01, 05-03 | COVERED | Selector and evidence panel. |
| REQ | QUAL-01 | Matches, importance, selection, and rank movement use non-color cues. | 05-01, 05-02, 05-03 | COVERED | Movement labels plus global accessibility polish. |
| RESEARCH | Phase 4 dependency checkpoint | Verify delivered metric state/API before final comparison. | 05-01 | COVERED | Task 1 hard-stops if Phase 4 completion summaries, successful verification evidence, or required tests are absent. |
| RESEARCH | Pure comparison selector | Join rankings in domain code rather than duplicate ranking logic in React. | 05-01 | COVERED | Task 2. |
| RESEARCH | Text-first movement cues | Use labels/icons, not color alone. | 05-01 | COVERED | Task 3. |
| CONTEXT | D-06/D-07 | Rank movement uses text plus ▲, ▼, or – rather than color alone. | 05-01 | COVERED | Task 3 renders text-first movement labels and supporting shape icons. |
| CONTEXT | D-08 | Keyword and semantic rankings are adjacent columns. | 05-01 | COVERED | Task 3 renders the two labelled ranking columns. |
| CONTEXT | D-09 | Selecting a row highlights the same document in both columns. | 05-01 | COVERED | Task 3 implements synchronized, accessible row selection. |
| CONTEXT | D-10 | Selection details show keyword TF-IDF on the left and active semantic calculations on the right. | 05-01 | COVERED | Task 3 renders the required two-column comparison card. |
| CONTEXT | Phase 4 D-01/D-02 | Toggle visible on final-comparison and backed by global reducer state. | 05-01 | COVERED | Task 1 verifies completed Phase 4 summaries plus tests for toggle visibility and persistence before wiring. |
</source_audit>

<verification>
- Run the Phase 4 preflight commands from Task 1 before editing Phase 5 code.
- Run `npm test -- src/domain/simulation.test.ts -t "final comparison|rank movement|semantic metric evidence"`.
- Run `npm test -- src/features/visualization-panel/final-comparison/FinalComparisonStep.test.tsx`.
- Run `npm test -- src/features/visualization-panel/VisualizationPanel.test.tsx -t "final comparison"`.
- Run `npm test -- src/App.test.tsx -t "final comparison"`.
- Run `npm test -- --run`.
</verification>

<success_criteria>
- COMP-01 is satisfied by side-by-side keyword and selected semantic rankings on `final-comparison`.
- COMP-02 is satisfied by every document showing keyword rank, semantic rank, and non-color movement text.
- COMP-03 is satisfied by explanations tied to visible keyword and selected semantic metric evidence.
- QUAL-01 is satisfied for rank movement in the final comparison.
- D-09 and D-10 are satisfied by accessible synchronized row selection and a detailed, side-by-side evidence card.
- Execution stops rather than absorbing Phase 4 work if completed Phase 4 summaries, successful verification evidence, required tests, or the delivered semantic metric API are missing.
</success_criteria>

<output>
Create `.planning/phases/05-final-comparison-and-release-readiness/05-01-SUMMARY.md` when done.
</output>
